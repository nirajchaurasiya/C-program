/*
    Specail Operator
    Comma (,)
    Sizeof (sizeof)
    * &
    . ->
*/
#include <stdio.h>
#include <conio.h>
void main()
{
    // int a, b, e;
    // a = 5, 4;
    // b = (5, 4);
    // int c = (5, 4);
    // printf("%d\t", a);
    // printf("%d\t", b);
    // printf("%d\t", c);
    // int d = (printf("Hello"), 2, 3);
    // e = a, b;
    // printf("\t%d", d);
    // printf("\t%d", e);
    // int a = 0, b = 1, c = -1, d;
    // d = --a * (5 + b) / 2 - c++ * b;
    // printf("%d", d);
    int a, b;
    scanf("%d%d%d", &a, &b);
}